<Window x:Class="PanoramaFuturesAI.Views.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="日志查看器"
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E6EDF3"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30363D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="AI 日志" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                <Border Background="#F85149" CornerRadius="10" Padding="8,2" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="ErrorCountText" Text="0" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="复制全部" Click="CopyAll_Click" Margin="0,0,8,0"/>
                <Button Content="清空" Click="Clear_Click"/>
            </StackPanel>
        </Grid>

        <!-- 日志列表 -->
        <Border Grid.Row="1" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl x:Name="LogList" Padding="8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#21262D" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- 时间戳和来源 -->
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                        <Border CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#F85149"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="Info">
                                                            <Setter Property="Background" Value="#58A6FF"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                            <Setter Property="Background" Value="#D29922"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding LevelDisplay}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Text="{Binding Source}" Foreground="#8B949E" FontSize="12" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='  ·  {0:yyyy-MM-dd HH:mm:ss}'}" Foreground="#484F58" FontSize="12" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- 错误消息 -->
                                    <TextBlock Grid.Row="1" Text="{Binding Message}" Foreground="#F85149" FontSize="13" TextWrapping="Wrap"/>

                                    <!-- 详情（如果有） -->
                                    <Border Grid.Row="2" Background="#0D1117" CornerRadius="4" Padding="8" Margin="0,8,0,0"
                                            Visibility="{Binding Details, Converter={StaticResource StringToVisibilityConverter}}">
                                        <TextBlock Text="{Binding Details}" Foreground="#8B949E" FontSize="12" TextWrapping="Wrap" FontFamily="Consolas"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- 底部状态 -->
        <TextBlock Grid.Row="2" x:Name="StatusText" Text="暂无日志" Foreground="#484F58" FontSize="12" Margin="0,12,0,0" HorizontalAlignment="Center"/>
    </Grid>
</Window>
